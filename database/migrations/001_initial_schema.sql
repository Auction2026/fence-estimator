-- Migration 001: Initial Schema
-- Run schema.sql to create all tables
-- \i database/schema.sql
-- Description: Creates all 9 tables, indexes, triggers, and views.
SELECT 'Migration 001 — run database/schema.sql to create initial schema.' AS info;
