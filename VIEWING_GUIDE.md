# Viewing Guide

- Frontend tabs are navigated from the top nav in `index.html`.
- Backend code is in `backend/server.js`.
- Database scripts live under `database/`.
- Wire diagrams are in `docs/WIRE_GRIDS/`.
