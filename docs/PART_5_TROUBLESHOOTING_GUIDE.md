# Fence Depot Estimator - Troubleshooting Guide

Use this guide when deployment, database, backend, frontend, security, or performance issues interrupt normal estimating operations.
For every issue, review the problem statement, compare symptoms, apply the step-by-step solution, and document the outcome.

## Database Issues

### Issue 1: Connection refused on port 5432
**Problem:** Connection refused on port 5432 is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Connection refused on port 5432".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the connection refused on port 5432 issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 2: Authentication failed for fence_user
**Problem:** Authentication failed for fence_user is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Authentication failed for fence_user".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the authentication failed for fence_user issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 3: Database does not exist
**Problem:** Database does not exist is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Database does not exist".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the database does not exist issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 4: Schema creation fails
**Problem:** Schema creation fails is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Schema creation fails".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the schema creation fails issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 5: Product seed fails
**Problem:** Product seed fails is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Product seed fails".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the product seed fails issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 6: Index already exists
**Problem:** Index already exists is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Index already exists".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the index already exists issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 7: Foreign key constraint violation
**Problem:** Foreign key constraint violation is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Foreign key constraint violation".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the foreign key constraint violation issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 8: Permission denied on schema public
**Problem:** Permission denied on schema public is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Permission denied on schema public".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the permission denied on schema public issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 9: Slow queries on projects
**Problem:** Slow queries on projects is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Slow queries on projects".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the slow queries on projects issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 10: Connection pool exhausted
**Problem:** Connection pool exhausted is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Connection pool exhausted".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the connection pool exhausted issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 11: Too many clients already
**Problem:** Too many clients already is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Too many clients already".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the too many clients already issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 12: SSL requirement mismatch
**Problem:** SSL requirement mismatch is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "SSL requirement mismatch".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the ssl requirement mismatch issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 13: Role does not exist
**Problem:** Role does not exist is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Role does not exist".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the role does not exist issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 14: Extension uuid-ossp missing
**Problem:** Extension uuid-ossp missing is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Extension uuid-ossp missing".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the extension uuid-ossp missing issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 15: Sequence out of sync
**Problem:** Sequence out of sync is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Sequence out of sync".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the sequence out of sync issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 16: Deadlock detected during updates
**Problem:** Deadlock detected during updates is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Deadlock detected during updates".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the deadlock detected during updates issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 17: Disk full during backup
**Problem:** Disk full during backup is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Disk full during backup".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the disk full during backup issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 18: Cannot restore custom-format dump
**Problem:** Cannot restore custom-format dump is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Cannot restore custom-format dump".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the cannot restore custom-format dump issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 19: Invalid input syntax for UUID
**Problem:** Invalid input syntax for UUID is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Invalid input syntax for UUID".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the invalid input syntax for uuid issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 20: Timestamp timezone confusion
**Problem:** Timestamp timezone confusion is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Timestamp timezone confusion".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the timestamp timezone confusion issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 21: Duplicate project numbers
**Problem:** Duplicate project numbers is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Duplicate project numbers".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the duplicate project numbers issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 22: Vacuum not keeping up
**Problem:** Vacuum not keeping up is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Vacuum not keeping up".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the vacuum not keeping up issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

## Backend Issues

### Issue 23: Server will not start
**Problem:** Server will not start is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Server will not start".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the server will not start issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 24: Port 3000 already in use
**Problem:** Port 3000 already in use is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Port 3000 already in use".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the port 3000 already in use issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 25: Cannot find module error
**Problem:** Cannot find module error is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Cannot find module error".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the cannot find module error issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 26: JWT_SECRET not set
**Problem:** JWT_SECRET not set is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "JWT_SECRET not set".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the jwt_secret not set issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 27: CORS error in browser
**Problem:** CORS error in browser is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "CORS error in browser".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the cors error in browser issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 28: 404 on API endpoints
**Problem:** 404 on API endpoints is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "404 on API endpoints".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the 404 on api endpoints issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 29: 500 internal server error
**Problem:** 500 internal server error is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "500 internal server error".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the 500 internal server error issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 30: Database pool error
**Problem:** Database pool error is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Database pool error".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the database pool error issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 31: File upload fails
**Problem:** File upload fails is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "File upload fails".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the file upload fails issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 32: Memory usage high
**Problem:** Memory usage high is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Memory usage high".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the memory usage high issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 33: Unhandled promise rejection
**Problem:** Unhandled promise rejection is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Unhandled promise rejection".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the unhandled promise rejection issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 34: Environment variables not loading
**Problem:** Environment variables not loading is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Environment variables not loading".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the environment variables not loading issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 35: Nodemon restart loop
**Problem:** Nodemon restart loop is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Nodemon restart loop".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the nodemon restart loop issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 36: PDF generation timeout
**Problem:** PDF generation timeout is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "PDF generation timeout".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the pdf generation timeout issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 37: Email service credentials rejected
**Problem:** Email service credentials rejected is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Email service credentials rejected".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the email service credentials rejected issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 38: Request body too large
**Problem:** Request body too large is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Request body too large".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the request body too large issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 39: Rate limiter blocks internal users
**Problem:** Rate limiter blocks internal users is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Rate limiter blocks internal users".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the rate limiter blocks internal users issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 40: Authentication middleware rejects valid token
**Problem:** Authentication middleware rejects valid token is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Authentication middleware rejects valid token".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the authentication middleware rejects valid token issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 41: Password hash comparison fails
**Problem:** Password hash comparison fails is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Password hash comparison fails".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the password hash comparison fails issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 42: Session logout not clearing token
**Problem:** Session logout not clearing token is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Session logout not clearing token".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the session logout not clearing token issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 43: Health check endpoint returns failure
**Problem:** Health check endpoint returns failure is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Health check endpoint returns failure".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the health check endpoint returns failure issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 44: API response fields missing
**Problem:** API response fields missing is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "API response fields missing".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the api response fields missing issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 45: Logging too verbose in production
**Problem:** Logging too verbose in production is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Logging too verbose in production".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the logging too verbose in production issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 46: Background job never completes
**Problem:** Background job never completes is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Background job never completes".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the background job never completes issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 47: Change order calculations inconsistent
**Problem:** Change order calculations inconsistent is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Change order calculations inconsistent".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the change order calculations inconsistent issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

## Frontend Issues

### Issue 48: Blank white screen on load
**Problem:** Blank white screen on load is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Blank white screen on load".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the blank white screen on load issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 49: Tabs not switching
**Problem:** Tabs not switching is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Tabs not switching".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the tabs not switching issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 50: Forms not submitting
**Problem:** Forms not submitting is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Forms not submitting".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the forms not submitting issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 51: Canvas drawing not working
**Problem:** Canvas drawing not working is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Canvas drawing not working".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the canvas drawing not working issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 52: Estimate not calculating
**Problem:** Estimate not calculating is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Estimate not calculating".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the estimate not calculating issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 53: Contract not showing
**Problem:** Contract not showing is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Contract not showing".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the contract not showing issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 54: PDF not generating
**Problem:** PDF not generating is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "PDF not generating".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the pdf not generating issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 55: localStorage not saving
**Problem:** localStorage not saving is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "localStorage not saving".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the localstorage not saving issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 56: API calls returning 401
**Problem:** API calls returning 401 is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "API calls returning 401".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the api calls returning 401 issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 57: API calls returning 404
**Problem:** API calls returning 404 is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "API calls returning 404".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the api calls returning 404 issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 58: Catalog not loading
**Problem:** Catalog not loading is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Catalog not loading".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the catalog not loading issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 59: Notes not saving
**Problem:** Notes not saving is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Notes not saving".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the notes not saving issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 60: Sign-off signature pad not recording
**Problem:** Sign-off signature pad not recording is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Sign-off signature pad not recording".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the sign-off signature pad not recording issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 61: Change order modal not opening
**Problem:** Change order modal not opening is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Change order modal not opening".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the change order modal not opening issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 62: Dropdown options missing
**Problem:** Dropdown options missing is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Dropdown options missing".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the dropdown options missing issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 63: Date picker format incorrect
**Problem:** Date picker format incorrect is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Date picker format incorrect".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the date picker format incorrect issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 64: Table sorting broken
**Problem:** Table sorting broken is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Table sorting broken".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the table sorting broken issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 65: Search filter not updating
**Problem:** Search filter not updating is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Search filter not updating".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the search filter not updating issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 66: Save button stays disabled
**Problem:** Save button stays disabled is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Save button stays disabled".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the save button stays disabled issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 67: Validation errors not visible
**Problem:** Validation errors not visible is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Validation errors not visible".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the validation errors not visible issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 68: Loading spinner never stops
**Problem:** Loading spinner never stops is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Loading spinner never stops".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the loading spinner never stops issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 69: Images not previewing
**Problem:** Images not previewing is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Images not previewing".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the images not previewing issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 70: Auto-fill customer data fails
**Problem:** Auto-fill customer data fails is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Auto-fill customer data fails".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the auto-fill customer data fails issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 71: Totals display NaN
**Problem:** Totals display NaN is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Totals display NaN".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the totals display nan issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 72: Print layout cut off
**Problem:** Print layout cut off is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Print layout cut off".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the print layout cut off issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 73: Dark mode colors unreadable
**Problem:** Dark mode colors unreadable is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Dark mode colors unreadable".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the dark mode colors unreadable issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 74: Navigation resets after refresh
**Problem:** Navigation resets after refresh is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Navigation resets after refresh".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the navigation resets after refresh issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 75: Schedule calendar missing entries
**Problem:** Schedule calendar missing entries is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Schedule calendar missing entries".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the schedule calendar missing entries issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 76: Reports chart not rendering
**Problem:** Reports chart not rendering is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Reports chart not rendering".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the reports chart not rendering issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 77: Browser back button loses edits
**Problem:** Browser back button loses edits is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Browser back button loses edits".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the browser back button loses edits issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

## Security Issues

### Issue 78: Weak password accepted
**Problem:** Weak password accepted is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Weak password accepted".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the weak password accepted issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 79: Session expiration too short
**Problem:** Session expiration too short is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Session expiration too short".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the session expiration too short issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 80: Session expiration too long
**Problem:** Session expiration too long is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Session expiration too long".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the session expiration too long issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 81: HTTPS not enabled
**Problem:** HTTPS not enabled is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "HTTPS not enabled".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the https not enabled issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 82: CSRF concerns
**Problem:** CSRF concerns is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "CSRF concerns".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the csrf concerns issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 83: Potential XSS in notes
**Problem:** Potential XSS in notes is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Potential XSS in notes".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the potential xss in notes issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 84: SQL injection prevention review
**Problem:** SQL injection prevention review is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "SQL injection prevention review".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the sql injection prevention review issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 85: Admin routes exposed to viewers
**Problem:** Admin routes exposed to viewers is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Admin routes exposed to viewers".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the admin routes exposed to viewers issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 86: Password reset link never expires
**Problem:** Password reset link never expires is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Password reset link never expires".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the password reset link never expires issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 87: Shared workstation auto-login risk
**Problem:** Shared workstation auto-login risk is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Shared workstation auto-login risk".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the shared workstation auto-login risk issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 88: Sensitive logs contain tokens
**Problem:** Sensitive logs contain tokens is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Sensitive logs contain tokens".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the sensitive logs contain tokens issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 89: File upload type restrictions missing
**Problem:** File upload type restrictions missing is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "File upload type restrictions missing".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the file upload type restrictions missing issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 90: Open CORS policy
**Problem:** Open CORS policy is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Open CORS policy".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the open cors policy issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 91: Outdated dependency vulnerability
**Problem:** Outdated dependency vulnerability is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Outdated dependency vulnerability".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the outdated dependency vulnerability issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 92: Missing security headers
**Problem:** Missing security headers is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Missing security headers".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the missing security headers issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 93: No account lockout after repeated failures
**Problem:** No account lockout after repeated failures is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "No account lockout after repeated failures".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the no account lockout after repeated failures issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 94: Backup files stored unencrypted
**Problem:** Backup files stored unencrypted is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Backup files stored unencrypted".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the backup files stored unencrypted issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 95: Contract PDFs accessible without auth
**Problem:** Contract PDFs accessible without auth is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Contract PDFs accessible without auth".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the contract pdfs accessible without auth issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

## Performance Issues

### Issue 96: Page loads slowly
**Problem:** Page loads slowly is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Page loads slowly".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the page loads slowly issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 97: Calculations take too long
**Problem:** Calculations take too long is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Calculations take too long".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the calculations take too long issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 98: Large file uploads stall
**Problem:** Large file uploads stall is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Large file uploads stall".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the large file uploads stall issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 99: Database query optimization needed
**Problem:** Database query optimization needed is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Database query optimization needed".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the database query optimization needed issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 100: Inventory search lagging
**Problem:** Inventory search lagging is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Inventory search lagging".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the inventory search lagging issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 101: Frontend bundle too large
**Problem:** Frontend bundle too large is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Frontend bundle too large".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the frontend bundle too large issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 102: Slow initial login
**Problem:** Slow initial login is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Slow initial login".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the slow initial login issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 103: Excessive API round trips
**Problem:** Excessive API round trips is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Excessive API round trips".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the excessive api round trips issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 104: Repeated re-rendering in the browser
**Problem:** Repeated re-rendering in the browser is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Repeated re-rendering in the browser".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the repeated re-rendering in the browser issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 105: Memory leak after long session
**Problem:** Memory leak after long session is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Memory leak after long session".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the memory leak after long session issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 106: Bulk product import too slow
**Problem:** Bulk product import too slow is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Bulk product import too slow".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the bulk product import too slow issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 107: PDF generation CPU spikes
**Problem:** PDF generation CPU spikes is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "PDF generation CPU spikes".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the pdf generation cpu spikes issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 108: Slow backup window
**Problem:** Slow backup window is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Slow backup window".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the slow backup window issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 109: Indexes fragmented
**Problem:** Indexes fragmented is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Indexes fragmented".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the indexes fragmented issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 110: Reports page heavy on old data
**Problem:** Reports page heavy on old data is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Reports page heavy on old data".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the reports page heavy on old data issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 111: Mobile browser sluggish
**Problem:** Mobile browser sluggish is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Mobile browser sluggish".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the mobile browser sluggish issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 112: Too many open browser tabs
**Problem:** Too many open browser tabs is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Too many open browser tabs".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the too many open browser tabs issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 113: Overfetching project details
**Problem:** Overfetching project details is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Overfetching project details".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the overfetching project details issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 114: Large notes history slows screen
**Problem:** Large notes history slows screen is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Large notes history slows screen".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the large notes history slows screen issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.

### Issue 115: Image thumbnails take too long
**Problem:** Image thumbnails take too long is preventing part of the Fence Depot Estimator workflow from completing as expected.

**Symptoms:**
- Users report the specific failure associated with "Image thumbnails take too long".
- Logs, browser tools, or command output indicate warnings or errors related to this problem area.
- The workflow either stops completely, behaves inconsistently, or produces incomplete results.

**Solution:**
1. Reproduce the image thumbnails take too long issue in a controlled way and capture the exact error message.
2. Verify recent configuration changes, credentials, package updates, or infrastructure changes that may have introduced the problem.
3. Apply the smallest safe fix first, such as correcting configuration values, restarting services, repairing permissions, or rolling back a recent change.
4. Re-test the affected workflow using a known-good sample project, estimate, or user account.
5. Document the root cause, the fix applied, and any preventive follow-up work for the team.

**Verification:**
- Confirm the error no longer appears and the affected workflow completes successfully.
- Confirm logs and monitoring return to normal after the fix.

**Prevention:**
- Add the scenario to your go-live checklist, maintenance checklist, or monitoring dashboard if it is likely to recur.
