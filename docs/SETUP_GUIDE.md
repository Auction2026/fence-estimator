
# Setup Guide

1. Open a terminal in `/home/runner/work/fence-estimator/fence-estimator/frontend`.
2. Install frontend packages with `npm install` when network access is available.
3. Start the static frontend with `npm run start`.
4. Open `http://localhost:8000` in your browser.
5. Load backend and database services as needed for API-backed workflows.
