# Project Lifecycle

## Overview
Project lifecycle states determine which records are editable, reportable, and ready for downstream actions.

## ASCII Diagram
```text
draft → estimate → contract → active → completed
   \                              ↘
    └────────────→ cancelled       archived reporting
```

## Detailed Explanation
### Detail 1
- Project Lifecycle detail 1 explains one operational viewpoint for the Fence Depot Estimator workflow.
- Teams should review detail 1 when validating changes to data ownership, status transitions, or user expectations.
- If a workflow step changes, update this wire grid so architecture and training documents stay aligned.

### Detail 2
- Project Lifecycle detail 2 explains one operational viewpoint for the Fence Depot Estimator workflow.
- Teams should review detail 2 when validating changes to data ownership, status transitions, or user expectations.
- If a workflow step changes, update this wire grid so architecture and training documents stay aligned.

### Detail 3
- Project Lifecycle detail 3 explains one operational viewpoint for the Fence Depot Estimator workflow.
- Teams should review detail 3 when validating changes to data ownership, status transitions, or user expectations.
- If a workflow step changes, update this wire grid so architecture and training documents stay aligned.

### Detail 4
- Project Lifecycle detail 4 explains one operational viewpoint for the Fence Depot Estimator workflow.
- Teams should review detail 4 when validating changes to data ownership, status transitions, or user expectations.
- If a workflow step changes, update this wire grid so architecture and training documents stay aligned.

### Detail 5
- Project Lifecycle detail 5 explains one operational viewpoint for the Fence Depot Estimator workflow.
- Teams should review detail 5 when validating changes to data ownership, status transitions, or user expectations.
- If a workflow step changes, update this wire grid so architecture and training documents stay aligned.

### Detail 6
- Project Lifecycle detail 6 explains one operational viewpoint for the Fence Depot Estimator workflow.
- Teams should review detail 6 when validating changes to data ownership, status transitions, or user expectations.
- If a workflow step changes, update this wire grid so architecture and training documents stay aligned.

### Detail 7
- Project Lifecycle detail 7 explains one operational viewpoint for the Fence Depot Estimator workflow.
- Teams should review detail 7 when validating changes to data ownership, status transitions, or user expectations.
- If a workflow step changes, update this wire grid so architecture and training documents stay aligned.

### Detail 8
- Project Lifecycle detail 8 explains one operational viewpoint for the Fence Depot Estimator workflow.
- Teams should review detail 8 when validating changes to data ownership, status transitions, or user expectations.
- If a workflow step changes, update this wire grid so architecture and training documents stay aligned.

### Detail 9
- Project Lifecycle detail 9 explains one operational viewpoint for the Fence Depot Estimator workflow.
- Teams should review detail 9 when validating changes to data ownership, status transitions, or user expectations.
- If a workflow step changes, update this wire grid so architecture and training documents stay aligned.

### Detail 10
- Project Lifecycle detail 10 explains one operational viewpoint for the Fence Depot Estimator workflow.
- Teams should review detail 10 when validating changes to data ownership, status transitions, or user expectations.
- If a workflow step changes, update this wire grid so architecture and training documents stay aligned.

### Detail 11
- Project Lifecycle detail 11 explains one operational viewpoint for the Fence Depot Estimator workflow.
- Teams should review detail 11 when validating changes to data ownership, status transitions, or user expectations.
- If a workflow step changes, update this wire grid so architecture and training documents stay aligned.

### Detail 12
- Project Lifecycle detail 12 explains one operational viewpoint for the Fence Depot Estimator workflow.
- Teams should review detail 12 when validating changes to data ownership, status transitions, or user expectations.
- If a workflow step changes, update this wire grid so architecture and training documents stay aligned.

### Detail 13
- Project Lifecycle detail 13 explains one operational viewpoint for the Fence Depot Estimator workflow.
- Teams should review detail 13 when validating changes to data ownership, status transitions, or user expectations.
- If a workflow step changes, update this wire grid so architecture and training documents stay aligned.

### Detail 14
- Project Lifecycle detail 14 explains one operational viewpoint for the Fence Depot Estimator workflow.
- Teams should review detail 14 when validating changes to data ownership, status transitions, or user expectations.
- If a workflow step changes, update this wire grid so architecture and training documents stay aligned.

### Detail 15
- Project Lifecycle detail 15 explains one operational viewpoint for the Fence Depot Estimator workflow.
- Teams should review detail 15 when validating changes to data ownership, status transitions, or user expectations.
- If a workflow step changes, update this wire grid so architecture and training documents stay aligned.

## Role Considerations
- **Admin:** Review how admin access affects the project lifecycle flow and which actions should be read-only versus editable.
- **Estimator:** Review how estimator access affects the project lifecycle flow and which actions should be read-only versus editable.
- **Installer:** Review how installer access affects the project lifecycle flow and which actions should be read-only versus editable.
- **Viewer:** Review how viewer access affects the project lifecycle flow and which actions should be read-only versus editable.

## Validation Checklist
- [ ] Validate checkpoint 1 for the Project Lifecycle diagram during implementation or QA review.
- [ ] Validate checkpoint 2 for the Project Lifecycle diagram during implementation or QA review.
- [ ] Validate checkpoint 3 for the Project Lifecycle diagram during implementation or QA review.
- [ ] Validate checkpoint 4 for the Project Lifecycle diagram during implementation or QA review.
- [ ] Validate checkpoint 5 for the Project Lifecycle diagram during implementation or QA review.
- [ ] Validate checkpoint 6 for the Project Lifecycle diagram during implementation or QA review.
- [ ] Validate checkpoint 7 for the Project Lifecycle diagram during implementation or QA review.
- [ ] Validate checkpoint 8 for the Project Lifecycle diagram during implementation or QA review.
- [ ] Validate checkpoint 9 for the Project Lifecycle diagram during implementation or QA review.
- [ ] Validate checkpoint 10 for the Project Lifecycle diagram during implementation or QA review.
- [ ] Validate checkpoint 11 for the Project Lifecycle diagram during implementation or QA review.
- [ ] Validate checkpoint 12 for the Project Lifecycle diagram during implementation or QA review.
- [ ] Validate checkpoint 13 for the Project Lifecycle diagram during implementation or QA review.
- [ ] Validate checkpoint 14 for the Project Lifecycle diagram during implementation or QA review.
- [ ] Validate checkpoint 15 for the Project Lifecycle diagram during implementation or QA review.
- [ ] Validate checkpoint 16 for the Project Lifecycle diagram during implementation or QA review.
- [ ] Validate checkpoint 17 for the Project Lifecycle diagram during implementation or QA review.
- [ ] Validate checkpoint 18 for the Project Lifecycle diagram during implementation or QA review.
- [ ] Validate checkpoint 19 for the Project Lifecycle diagram during implementation or QA review.
- [ ] Validate checkpoint 20 for the Project Lifecycle diagram during implementation or QA review.

## Notes for Future Updates
1. Keep project lifecycle note 1 synchronized with the live application behavior, API responses, and database rules.
2. Keep project lifecycle note 2 synchronized with the live application behavior, API responses, and database rules.
3. Keep project lifecycle note 3 synchronized with the live application behavior, API responses, and database rules.
4. Keep project lifecycle note 4 synchronized with the live application behavior, API responses, and database rules.
5. Keep project lifecycle note 5 synchronized with the live application behavior, API responses, and database rules.
6. Keep project lifecycle note 6 synchronized with the live application behavior, API responses, and database rules.
7. Keep project lifecycle note 7 synchronized with the live application behavior, API responses, and database rules.
8. Keep project lifecycle note 8 synchronized with the live application behavior, API responses, and database rules.
9. Keep project lifecycle note 9 synchronized with the live application behavior, API responses, and database rules.
10. Keep project lifecycle note 10 synchronized with the live application behavior, API responses, and database rules.
11. Keep project lifecycle note 11 synchronized with the live application behavior, API responses, and database rules.
12. Keep project lifecycle note 12 synchronized with the live application behavior, API responses, and database rules.
13. Keep project lifecycle note 13 synchronized with the live application behavior, API responses, and database rules.
14. Keep project lifecycle note 14 synchronized with the live application behavior, API responses, and database rules.
15. Keep project lifecycle note 15 synchronized with the live application behavior, API responses, and database rules.
16. Keep project lifecycle note 16 synchronized with the live application behavior, API responses, and database rules.
17. Keep project lifecycle note 17 synchronized with the live application behavior, API responses, and database rules.
18. Keep project lifecycle note 18 synchronized with the live application behavior, API responses, and database rules.
19. Keep project lifecycle note 19 synchronized with the live application behavior, API responses, and database rules.
20. Keep project lifecycle note 20 synchronized with the live application behavior, API responses, and database rules.
21. Keep project lifecycle note 21 synchronized with the live application behavior, API responses, and database rules.
22. Keep project lifecycle note 22 synchronized with the live application behavior, API responses, and database rules.
23. Keep project lifecycle note 23 synchronized with the live application behavior, API responses, and database rules.
24. Keep project lifecycle note 24 synchronized with the live application behavior, API responses, and database rules.
25. Keep project lifecycle note 25 synchronized with the live application behavior, API responses, and database rules.
