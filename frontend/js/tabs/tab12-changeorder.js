function escapeChangeOrderHtml(value){return String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]))}function initTab12(){loadChangeOrderData()}function loadChangeOrderData(){const proj=storage.getCurrentProject();if(!proj)return;const changes=proj.changeOrders||[];displayChangeOrderTable(changes)}function displayChangeOrderTable(changes){const tbody=document.querySelector('#tab-tab12 .data-table tbody');if(!tbody)return;tbody.innerHTML='';changes.forEach((c,i)=>{const tr=document.createElement('tr');tr.innerHTML=`<td>#CO${c.number||i+1}</td><td>${escapeChangeOrderHtml(c.description)}</td><td>$${(parseFloat(c.amount)||0).toFixed(2)}</td><td>${escapeChangeOrderHtml(c.status||'pending')}</td><td><button class="btn btn-sm" onclick="updateChangeOrder(${i})">Update</button><button class="btn btn-danger btn-sm" onclick="deleteChangeOrder(${i})">Delete</button></td>`;tbody.appendChild(tr)})}function addChangeOrder(){const form=document.getElementById('changeOrderForm');if(!form)return;const data={number:Date.now(),description:form.changeDescription.value,amount:parseFloat(form.changeAmount.value),status:'pending',date:new Date().toISOString()};const proj=storage.getCurrentProject();if(proj){if(!proj.changeOrders)proj.changeOrders=[];proj.changeOrders.push(data);storage.saveProject(proj);displayChangeOrderTable(proj.changeOrders);form.reset();ui.showNotification('Change order added!','success')}}function updateChangeOrder(){ui.showNotification('Change order updates are not implemented yet.','info')}function deleteChangeOrder(index){const proj=storage.getCurrentProject();if(!proj||!proj.changeOrders)return;proj.changeOrders.splice(index,1);storage.saveProject(proj);displayChangeOrderTable(proj.changeOrders)}document.addEventListener('DOMContentLoaded',initTab12);window.addChangeOrder=addChangeOrder;window.updateChangeOrder=updateChangeOrder;window.deleteChangeOrder=deleteChangeOrder;
