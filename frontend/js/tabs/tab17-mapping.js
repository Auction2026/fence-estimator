function initTab17(){initMap()}let mapCanvas,mapCtx,isDrawingMap=false;function initMap(){mapCanvas=document.getElementById('propertyMap');if(mapCanvas){mapCtx=mapCanvas.getContext('2d');setupMapEvents();drawMapGrid()}}function setupMapEvents(){mapCanvas.addEventListener('mousedown',startMapDrawing);mapCanvas.addEventListener('mousemove',drawOnMap);mapCanvas.addEventListener('mouseup',stopMapDrawing);mapCanvas.addEventListener('mouseout',stopMapDrawing)}function startMapDrawing(e){isDrawingMap=true;const rect=mapCanvas.getBoundingClientRect();const x=e.clientX-rect.left;const y=e.clientY-rect.top;mapCtx.beginPath();mapCtx.moveTo(x,y)}function drawOnMap(e){if(!isDrawingMap)return;const rect=mapCanvas.getBoundingClientRect();const x=e.clientX-rect.left;const y=e.clientY-rect.top;mapCtx.lineWidth=3;mapCtx.lineCap='round';mapCtx.strokeStyle='#ff6b6b';mapCtx.lineTo(x,y);mapCtx.stroke()}function stopMapDrawing(){isDrawingMap=false;mapCtx.closePath()}function drawMapGrid(){mapCtx.strokeStyle='#e0e0e0';mapCtx.lineWidth=1;for(let i=0;i<=mapCanvas.width;i+=50){mapCtx.beginPath();mapCtx.moveTo(i,0);mapCtx.lineTo(i,mapCanvas.height);mapCtx.stroke()}for(let i=0;i<=mapCanvas.height;i+=50){mapCtx.beginPath();mapCtx.moveTo(0,i);mapCtx.lineTo(mapCanvas.width,i);mapCtx.stroke()}}function clearMapCanvas(){mapCtx.clearRect(0,0,mapCanvas.width,mapCanvas.height);drawMapGrid();ui.showNotification('Map cleared','info')}document.addEventListener('DOMContentLoaded',initTab17);window.clearMapCanvas=clearMapCanvas;
