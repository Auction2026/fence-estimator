function initTab6(){const form=document.getElementById('permitsForm');if(form){form.addEventListener('submit',savePermits)}}function savePermits(e){e.preventDefault();const data={permitNumber:document.getElementById('permitNumber').value,permitStatus:document.getElementById('permitStatus').value};storage.saveData('permits',data);ui.showNotification('Permit info saved!','success')}function loadTab6Data(){const data=storage.getData('permits');if(data){document.getElementById('permitNumber').value=data.permitNumber||'';document.getElementById('permitStatus').value=data.permitStatus||'pending'}}document.addEventListener('DOMContentLoaded',()=>{initTab6();loadTab6Data()});window.savePermits=savePermits;window.loadTab6Data=loadTab6Data;