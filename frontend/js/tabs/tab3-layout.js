let layoutCanvas,layoutCtx,isDrawing=false;function initTab3(){layoutCanvas=document.getElementById('layoutCanvas');if(layoutCanvas){layoutCtx=layoutCanvas.getContext('2d');setupCanvasEvents()}}function setupCanvasEvents(){layoutCanvas.addEventListener('mousedown',startDrawingLayout);layoutCanvas.addEventListener('mousemove',drawOnCanvas);layoutCanvas.addEventListener('mouseup',stopDrawingLayout);layoutCanvas.addEventListener('mouseout',stopDrawingLayout)}function startDrawingLayout(e){isDrawing=true;const rect=layoutCanvas.getBoundingClientRect();const x=e.clientX-rect.left;const y=e.clientY-rect.top;layoutCtx.beginPath();layoutCtx.moveTo(x,y)}function drawOnCanvas(e){if(!isDrawing)return;const rect=layoutCanvas.getBoundingClientRect();const x=e.clientX-rect.left;const y=e.clientY-rect.top;layoutCtx.lineWidth=2;layoutCtx.lineCap='round';layoutCtx.strokeStyle='#0066cc';layoutCtx.lineTo(x,y);layoutCtx.stroke()}function stopDrawingLayout(){isDrawing=false;layoutCtx.closePath()}function clearCanvasLayout(){layoutCtx.clearRect(0,0,layoutCanvas.width,layoutCanvas.height);ui.showNotification('Canvas cleared','info')}function saveDrawingLayout(){const imageData=layoutCanvas.toDataURL('image/png');const proj=storage.getCurrentProject();if(proj){proj.layoutDrawing=imageData;storage.saveProject(proj);ui.showNotification('Drawing saved!','success')}}document.addEventListener('DOMContentLoaded',initTab3);window.clearCanvasLayout=clearCanvasLayout;window.saveDrawingLayout=saveDrawingLayout;