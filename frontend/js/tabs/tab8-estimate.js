function initTab8(){}function generatePDF(){ui.showNotification('PDF generation started...','success');setTimeout(()=>{ui.showNotification('Estimate PDF ready!','success')},1000)}function lockPrice(){if(confirm('Lock price?')){appState.isPriceLocked=true;storage.saveData('priceLocked',true);ui.showNotification('Price locked.','success')}}document.addEventListener('DOMContentLoaded',()=>{initTab8()});window.generatePDF=generatePDF;window.lockPrice=lockPrice;