function initTab13(){loadSignoffData()}function loadSignoffData(){const proj=storage.getCurrentProject();if(!proj)return;displaySignoffForm(proj)}function displaySignoffForm(proj){const form=document.getElementById('signoffForm');if(!form)return;form.innerHTML=`<div style="padding:20px;border:1px solid #ddd;border-radius:4px;"><h3>Project Completion Sign-Off</h3><p><strong>Project:</strong> ${proj.name||'---'}</p><p><strong>Address:</strong> ${proj.address||'---'}</p><p><strong>Completion Date:</strong> <input type="date" id="completionDate" value="${proj.completionDate||new Date().toISOString().split('T')[0]}"></p><p><strong>Inspector:</strong> <input type="text" id="inspector" value="${proj.inspector||''}"></p><p><strong>Notes:</strong> <textarea id="signoffNotes" style="width:100%;height:100px;">${proj.signoffNotes||''}</textarea></p><p><strong>Signature:</strong> <input type="text" id="signoffSignature" placeholder="Enter signature"></p><button class="btn btn-primary" onclick="completeSignoff()">Complete Sign-Off</button></div>`}function completeSignoff(){const proj=storage.getCurrentProject();if(!proj)return;proj.completionDate=document.getElementById('completionDate').value;proj.inspector=document.getElementById('inspector').value;proj.signoffNotes=document.getElementById('signoffNotes').value;proj.signoffSignature=document.getElementById('signoffSignature').value;proj.signoffDate=new Date().toISOString();storage.saveProject(proj);ui.showNotification('Project signed off!','success')}document.addEventListener('DOMContentLoaded',initTab13);window.completeSignoff=completeSignoff;
