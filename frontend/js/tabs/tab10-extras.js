function escapeExtraHtml(value){return String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]))}function initTab10(){loadExtrasData()}function loadExtrasData(){const proj=storage.getCurrentProject();if(!proj)return;const extras=proj.extras||[];displayExtrasTable(extras);updateExtrasTotal()}function displayExtrasTable(extras){const tbody=document.querySelector('#tab-tab10 .data-table tbody');if(!tbody)return;tbody.innerHTML='';extras.forEach((e,i)=>{const tr=document.createElement('tr');tr.innerHTML=`<td>${escapeExtraHtml(e.description)}</td><td>$${(parseFloat(e.price)||0).toFixed(2)}</td><td><button class="btn btn-sm" onclick="removeExtra(${i})">Remove</button></td>`;tbody.appendChild(tr)})}function addExtra(){const form=document.getElementById('extraForm');if(!form)return;const data={description:form.extraDescription.value,price:parseFloat(form.extraPrice.value)};const proj=storage.getCurrentProject();if(proj){if(!proj.extras)proj.extras=[];proj.extras.push(data);storage.saveProject(proj);displayExtrasTable(proj.extras);updateExtrasTotal();form.reset();ui.showNotification('Extra added!','success')}}function removeExtra(index){const proj=storage.getCurrentProject();if(!proj||!proj.extras)return;proj.extras.splice(index,1);storage.saveProject(proj);displayExtrasTable(proj.extras);updateExtrasTotal()}function updateExtrasTotal(){const proj=storage.getCurrentProject();if(!proj||!proj.extras)return;const total=proj.extras.reduce((sum,e)=>sum+parseFloat(e.price||0),0);const elem=document.getElementById('extrasTotal');if(elem)elem.textContent='$'+total.toFixed(2)}document.addEventListener('DOMContentLoaded',initTab10);window.addExtra=addExtra;window.removeExtra=removeExtra;
