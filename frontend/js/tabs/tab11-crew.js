function initTab11(){}function addCrewMember(e){e.preventDefault();const name=document.getElementById('crewName').value;const role=document.getElementById('crewRole').value;if(name&&role){appState.crew.push({name,role});saveAppState();updateCrewTable();document.getElementById('crewName').value='';ui.showNotification('Crew added','success')}}function removeCrewMember(i){appState.crew.splice(i,1);saveAppState();updateCrewTable()}function updateCrewTable(){const tbody=document.getElementById('crewTableBody');if(!tbody)return;tbody.innerHTML='';appState.crew.forEach((c,i)=>{const tr=document.createElement('tr');tr.innerHTML=`<td>${c.name}</td><td>${c.role}</td><td><button class="btn btn-danger btn-sm" onclick="removeCrewMember(${i})">Remove</button></td>`;tbody.appendChild(tr)})}document.addEventListener('DOMContentLoaded',()=>{initTab11();updateCrewTable()});window.addCrewMember=addCrewMember;window.removeCrewMember=removeCrewMember;