function initTab14(){}function addNote(e){e.preventDefault();const note=document.getElementById('noteContent').value;if(note){appState.notes.push({text:note,date:new Date().toLocaleString()});saveAppState();updateNotesList();document.getElementById('noteContent').value='';ui.showNotification('Note added','success')}}function removeNote(i){appState.notes.splice(i,1);saveAppState();updateNotesList()}function updateNotesList(){const list=document.getElementById('notesList');if(!list)return;list.innerHTML='';appState.notes.forEach((n,i)=>{const div=document.createElement('div');div.className='note-card';div.innerHTML=`<p><small>${n.date}</small></p><p>${n.text}</p><button class="btn btn-danger btn-sm" onclick="removeNote(${i})">Remove</button><hr>`;list.appendChild(div)})}document.addEventListener('DOMContentLoaded',()=>{initTab14();updateNotesList()});window.addNote=addNote;window.removeNote=removeNote;