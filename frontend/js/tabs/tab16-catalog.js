const mockProducts=[{sku:'CL-01',name:'Chain Link Fabric 4ft',price:15.50,stock:100},{sku:'WD-02',name:'Wood Panel 6x8',price:45.00,stock:50},{sku:'VN-03',name:'Vinyl Post 4x4x8',price:35.00,stock:200}];function initTab16(){const search=document.getElementById('productSearch');if(search){search.addEventListener('input',e=>filterProducts(e.target.value))}}function filterProducts(query){const q=query.toLowerCase();const filtered=mockProducts.filter(p=>p.name.toLowerCase().includes(q)||p.sku.toLowerCase().includes(q));renderProducts(filtered)}function renderProducts(products){const tbody=document.getElementById('productTableBody');if(!tbody)return;tbody.innerHTML='';products.forEach(p=>{const tr=document.createElement('tr');tr.innerHTML=`<td>${p.sku}</td><td>${p.name}</td><td>$${p.price.toFixed(2)}</td><td>${p.stock}</td>`;tbody.appendChild(tr)})}document.addEventListener('DOMContentLoaded',()=>{initTab16();renderProducts(mockProducts)});window.filterProducts=filterProducts;