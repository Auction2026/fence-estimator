function initTab7(){const form=document.getElementById('utilitiesForm');if(form){form.addEventListener('submit',saveUtilities)}}function saveUtilities(e){e.preventDefault();const utilities=[];document.querySelectorAll('input[name="utility"]:checked').forEach(cb=>utilities.push(cb.value));storage.saveData('utilities',utilities);ui.showNotification('Utilities info saved!','success')}function loadTab7Data(){const utilities=storage.getData('utilities')||[];document.querySelectorAll('input[name="utility"]').forEach(cb=>{cb.checked=utilities.includes(cb.value)})}document.addEventListener('DOMContentLoaded',()=>{initTab7();loadTab7Data()});window.saveUtilities=saveUtilities;window.loadTab7Data=loadTab7Data;