function initTab4(){loadInstallationData()}function loadInstallationData(){const proj=storage.getCurrentProject();if(!proj)return;const specs=proj.specs||{};const data=[{task:'Site Preparation',hours:4,rate:50,total:200},{task:'Digging & Posts',hours:8,rate:50,total:400},{task:'Fence Installation',hours:16,rate:50,total:800},{task:'Gate Installation',hours:specs.numberOfGates||0*4,rate:50,total:(specs.numberOfGates||0)*200},{task:'Cleanup',hours:4,rate:50,total:200}];displayInstallationTable(data)}function displayInstallationTable(data){const tbody=document.querySelector('[data-tab="installation"] tbody');if(!tbody){const t=document.querySelector('#tab-tab4 .data-table tbody');if(t){t.innerHTML='';data.forEach(r=>{const tr=document.createElement('tr');tr.innerHTML=`<td>${r.task}</td><td>${r.hours}</td><td>\$${r.rate}/hr</td><td>\$${r.total}</td>`;t.appendChild(tr)})}}else{tbody.innerHTML='';data.forEach(r=>{const tr=document.createElement('tr');tr.innerHTML=`<td>${r.task}</td><td>${r.hours}</td><td>\$${r.rate}/hr</td><td>\$${r.total}</td>`;tbody.appendChild(tr)})}}document.addEventListener('DOMContentLoaded',initTab4);window.loadInstallationData=loadInstallationData;