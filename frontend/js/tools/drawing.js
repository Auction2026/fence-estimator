class DrawingTool{constructor(canvasId){this.canvas=document.getElementById(canvasId);if(!this.canvas)throw new Error(`Canvas not found: ${canvasId}`);this.ctx=this.canvas.getContext('2d');this.isDrawing=false;this.color='#0066cc';this.lineWidth=2}startDrawing(e){this.isDrawing=true;const rect=this.canvas.getBoundingClientRect();const x=e.clientX-rect.left;const y=e.clientY-rect.top;this.ctx.beginPath();this.ctx.moveTo(x,y)}draw(e){if(!this.isDrawing)return;const rect=this.canvas.getBoundingClientRect();const x=e.clientX-rect.left;const y=e.clientY-rect.top;this.ctx.lineWidth=this.lineWidth;this.ctx.lineCap='round';this.ctx.strokeStyle=this.color;this.ctx.lineTo(x,y);this.ctx.stroke()}stopDrawing(){this.isDrawing=false;this.ctx.closePath()}drawRectangle(x,y,width,height){this.ctx.strokeStyle=this.color;this.ctx.lineWidth=this.lineWidth;this.ctx.strokeRect(x,y,width,height)}drawCircle(x,y,radius){this.ctx.strokeStyle=this.color;this.ctx.lineWidth=this.lineWidth;this.ctx.beginPath();this.ctx.arc(x,y,radius,0,2*Math.PI);this.ctx.stroke()}drawLine(x1,y1,x2,y2){this.ctx.strokeStyle=this.color;this.ctx.lineWidth=this.lineWidth;this.ctx.beginPath();this.ctx.moveTo(x1,y1);this.ctx.lineTo(x2,y2);this.ctx.stroke()}drawArrow(fromX,fromY,toX,toY){const headlen=15;const angle=Math.atan2(toY-fromY,toX-fromX);this.drawLine(fromX,fromY,toX,toY);this.ctx.fillStyle=this.color;this.ctx.beginPath();this.ctx.moveTo(toX,toY);this.ctx.lineTo(toX-headlen*Math.cos(angle-Math.PI/6),toY-headlen*Math.sin(angle-Math.PI/6));this.ctx.lineTo(toX-headlen*Math.cos(angle+Math.PI/6),toY-headlen*Math.sin(angle+Math.PI/6));this.ctx.closePath();this.ctx.fill()}addText(x,y,text,fontSize=14){this.ctx.fillStyle=this.color;this.ctx.font=`${fontSize}px Arial`;this.ctx.fillText(text,x,y)}setColor(color){this.color=color}setLineWidth(width){this.lineWidth=width}clear(){this.ctx.clearRect(0,0,this.canvas.width,this.canvas.height)}saveAsImage(){return this.canvas.toDataURL('image/png')}saveAsJSON(){const imageData=this.saveAsImage();return JSON.stringify({timestamp:new Date().toISOString(),image:imageData})}}let drawingTool=null;function initializeDrawingTool(canvasId='layoutCanvas'){const canvas=document.getElementById(canvasId);if(!canvas)return null;drawingTool=new DrawingTool(canvasId);window.drawingTool=drawingTool;return drawingTool}if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',()=>initializeDrawingTool())}else{initializeDrawingTool()}window.DrawingTool=DrawingTool;
