{
  "name": "fence-estimator-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend assets for Fence Estimator",
  "scripts": {
    "start": "python3 -m http.server 4173",
    "check": "node --check js/app.js && node --check js/api.js && node --check js/calculations.js && node --check js/validation.js"
  }
}
