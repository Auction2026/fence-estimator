# Fence Estimator - Programmer Handoff

This repository now includes both:

1. **The full code files that currently make up the application**
2. **A written guide a programmer can follow**

## Start Here

Open these files first:

- `/home/runner/work/fence-estimator/fence-estimator/FULL_PROGRAM_PACKAGE.md`
- `/home/runner/work/fence-estimator/fence-estimator/index-professional.html`
- `/home/runner/work/fence-estimator/fence-estimator/index.html`

## Which file is the main review copy?

- `index-professional.html` = the more detailed professional estimator interface
- `index.html` = the simpler estimator/demo version

## How to run the code

### Easiest way
1. Open the repository folder.
2. Double-click `index-professional.html` or `index.html`.
3. The app opens in a browser.

### Optional local server
From the repository folder run:

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000/`.

## Written guide

The full written guide is here:

- `/home/runner/work/fence-estimator/fence-estimator/FULL_PROGRAM_PACKAGE.md`

It explains:

- what each file does
- how the screens are organized
- where the calculations live
- how a programmer can extend the project
- troubleshooting steps
